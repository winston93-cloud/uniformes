import { TABLAS_MIGRACION_ORDER } from '@/lib/migracion/tablasOrder';

/** Tablas del sistema Uniformes en orden de migración. */
export const TABLAS_UNIFORMES = [...TABLAS_MIGRACION_ORDER] as const;
